import 'package:flutter/material.dart';

class NoActivity extends StatelessWidget {
  NoActivity({Key key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container(
      child: Center(
        child: Text("No Activity Widget"),
      ),
    );
  }
}
